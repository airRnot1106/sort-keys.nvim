description = "Lua handler"

can_sort_object = true
can_sort_array = true
can_deep = true

# Lua line/block comments are folded onto the field they document.
comment_aware = true

query_file = "sort-keys.scm"
